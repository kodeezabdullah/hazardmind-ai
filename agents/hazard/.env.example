# Featherless (PRIMARY) — backs both the Band LangGraph adapter (per-turn LLM)
# and the intelligence layer. Required.
FEATHERLESS_API_KEY=
NEON_DATABASE_URL=

# AIML + GPT last-resort fallback in shared/utils/llm_fallback.py only.
AIML_API_KEY=
OPENAI_BASE_URL=https://api.aimlapi.com/v1

# Gemini (primary escalation after Featherless) and an optional backup key.
# On a 429/quota-exceeded from GEMINI_API_KEY, the same request is retried ONCE
# against GEMINI_API_KEY_BACKUP before the chain moves on to Claude. Leave blank
# to disable the backup rotation. PLACEHOLDER ONLY — put the real key in .env.
GEMINI_API_KEY=
# GEMINI_API_KEY_BACKUP=
GPT_FALLBACK_MODEL=gpt-5.5-2026-04-23

# Band SDK
THENVOI_REST_URL=https://app.band.ai/
THENVOI_WS_URL=wss://app.band.ai/api/v1/socket/websocket
BAND_AGENT_ID=
BAND_API_KEY=
# ANTHROPIC_API_KEY — optional. The Band adapter now uses Featherless; this is
# only consulted by the AIML/Anthropic-compatible fallback path if configured.
# ANTHROPIC_API_KEY=
